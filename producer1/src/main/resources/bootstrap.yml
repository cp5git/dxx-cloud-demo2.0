server:
  port: 9081

logging:
  level:
    ROOT: info

spring:
  application:
    name: producer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

feign:
  hystrix:
    enabled: true
